{
  "name": "utme-mastery",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run utme-mobile\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "utme-mobile": "cd utme-mobile && npm start",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../utme-mobile && npm install",
    "build-all": "cd backend && npm run build && cd ../frontend && npm run build",
    "db:studio": "cd backend && npm run db:studio"
  },
  "devDependencies": {
    "@types/react": "^19.1.9",
    "concurrently": "^8.2.2"
  }
}
